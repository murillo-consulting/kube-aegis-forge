# Demo API

This deliberately small FastAPI service gives the platform a real workload to
build, scan, sign, deploy, observe, and recover. The repository root README is
the canonical operating guide.

